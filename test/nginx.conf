
events {} # event context needs to be defined to consider config valid

http {
  server {
    listen 80 default_server;
  
    location / {
      root /var/www/html/;
      index index.php; 
    }

  }
}